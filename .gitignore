# Variables de entorno: nunca al repositorio
.env
.env.*
!.env.example

node_modules/

# package-lock.json SÍ va al repositorio: fija las versiones exactas para que
# el build del servidor sea igual al local.
.DS_Store
dist

# Importaciones con datos reales del negocio (RUC, nombres de personas).
# Se quedan en local: publicarlas dejaría esa información en el historial
# del repositorio para siempre.
src/migrations/seed-sofianne-notes.ts
